<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:forceDarkAllowed="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnikoSheba">
        <activity
            android:name=".ui.services.ServiceSearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.checkout.CheckoutActivity"
            android:exported="false" />
        <activity
            android:name=".cart.SubmitOrderActivity"
            android:exported="true"
            android:theme="@style/Theme.UnikoSheba" />
        <activity
            android:name=".cart.CartListActivity"
            android:exported="true"
            android:theme="@style/Theme.UnikoSheba" />
        <activity
            android:name=".ui.services.ItemDetailsActivity"
            android:exported="true"
            android:theme="@style/Theme.UnikoSheba" />
        <activity
            android:name=".ui.services.ServicesActivity"
            android:exported="true"
            android:theme="@style/Theme.UnikoSheba" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UnikoSheba">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashActivity"
            android:exported="true" /> <!-- <activity -->
        <!-- android:name=".credential.ForgetPasswordActivity" -->
        <!-- android:exported="true" /> -->
        <activity
            android:name=".credential.RegisterActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".credential.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize|stateHidden" />
    </application>

</manifest>